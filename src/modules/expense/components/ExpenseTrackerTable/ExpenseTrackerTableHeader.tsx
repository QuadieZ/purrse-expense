import { Typography } from '@/common/ui';

export const ExpenseTrackerTableHeader = () => (
  <Typography
    variant="h1"
    fontSize="2xl"
    fontWeight="semibold">
    Expense Tracker
  </Typography>
);
